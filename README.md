## Implementando metodo SOLID em Typescript

### Install

```zsh

$ git init

$ yarn init -y
$ yarn add typescript ts-node-dev -D
$ yarn tsc --init

$ yarn add express
$ yarn add @types/express -D
$ yarn add uuidv4

```